import { ComponentProps } from 'react';
import { WalletModalButton } from '@solana/wallet-adapter-react-ui';

export interface WalletModalButtonProps extends ComponentProps<typeof WalletModalButton> {}
